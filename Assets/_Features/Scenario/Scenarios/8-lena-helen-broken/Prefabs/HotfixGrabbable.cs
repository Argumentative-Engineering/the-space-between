using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HotfixGrabbable : MonoBehaviour
{
    void Start()
    {
        // if (TryGetComponent(out FixedJoint joint))
        //     joint.connectedBody = PlayerInventory.Instance.ThrownTether.GetComponent<Rigidbody>();
    }

    void Update()
    {

    }
}
